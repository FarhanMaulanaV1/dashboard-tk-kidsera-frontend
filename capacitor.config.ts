import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kidseradashboard.v1',
  appName: 'kidseradashboard-v1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
